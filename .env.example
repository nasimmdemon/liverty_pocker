# Monitor dashboard (/monitor) — single shared login; both quick-login buttons use these values.
# Copy to `.env.local` for development. Set the same keys in your host (e.g. Vercel) for production.
VITE_MONITOR_LOGIN_EMAIL=
VITE_MONITOR_LOGIN_PASSWORD=
